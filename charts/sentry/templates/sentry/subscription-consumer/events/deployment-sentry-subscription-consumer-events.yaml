{{- if .Values.sentry.subscriptionConsumerEvents.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "sentry.fullname" . }}-subscription-consumer-events
  labels:
    app: sentry
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    {{- include "sentry.component.labels" (dict "component" "sentry-subscription-consumer-events" "ctx" .) | nindent 4 }}
  {{- if .Values.asHook }}
  {{- /* Add the Helm annotations so that deployment after asHook from true to false works */}}
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    "helm.sh/hook": "post-install,post-upgrade"
    "helm.sh/hook-weight": "10"
  {{- end }}
spec:
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
        app: sentry
        release: "{{ .Release.Name }}"
        role: sentry-subscription-consumer-events
  replicas: {{ .Values.sentry.subscriptionConsumerEvents.replicas }}
  template:
    metadata:
      annotations:
        checksum/configYml: {{ .Values.config.configYml | toYaml | toString | sha256sum }}
        checksum/sentryConfPy: {{ .Values.config.sentryConfPy | sha256sum }}
        checksum/config.yaml: {{ include "sentry.config" . | sha256sum }}
        {{- if .Values.sentry.subscriptionConsumerEvents.annotations }}
{{ toYaml .Values.sentry.subscriptionConsumerEvents.annotations | indent 8 }}
        {{- end }}
      labels:
        app: sentry
        release: "{{ .Release.Name }}"
        role: sentry-subscription-consumer-events
        {{- include "sentry.component.labels" (dict "component" "sentry-subscription-consumer-events" "ctx" .) | nindent 8 }}
        {{- if .Values.sentry.subscriptionConsumerEvents.podLabels }}
{{ toYaml .Values.sentry.subscriptionConsumerEvents.podLabels | indent 8 }}
        {{- end }}
    spec:
      affinity:
      {{- if .Values.sentry.subscriptionConsumerEvents.affinity }}
{{ toYaml .Values.sentry.subscriptionConsumerEvents.affinity | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.subscriptionConsumerEvents.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.sentry.subscriptionConsumerEvents.nodeSelector | indent 8 }}
      {{- else if .Values.global.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.global.nodeSelector | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.subscriptionConsumerEvents.tolerations }}
      tolerations:
{{ toYaml .Values.sentry.subscriptionConsumerEvents.tolerations | indent 8 }}
      {{- else if .Values.global.tolerations }}
      tolerations:
{{ toYaml .Values.global.tolerations | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.subscriptionConsumerEvents.topologySpreadConstraints }}
      topologySpreadConstraints:
{{ toYaml .Values.sentry.subscriptionConsumerEvents.topologySpreadConstraints | indent 8 }}
      {{- end }}
      {{- if .Values.images.sentry.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.images.sentry.imagePullSecrets | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.subscriptionConsumerEvents.securityContext }}
      securityContext:
{{ toYaml .Values.sentry.subscriptionConsumerEvents.securityContext | indent 8 }}
      {{- end }}
      initContainers:
{{ include "sentry.initContainer.nodestore-s3" . | indent 6 }}
      containers:
      - name: {{ .Chart.Name }}-subscription-consumer-events
        image: "{{ template "sentry.image" . }}"
        imagePullPolicy: {{ default "IfNotPresent" .Values.images.sentry.pullPolicy }}
        command: ["sentry"]
        args:
          - "run"
          - "consumer"
          - "events-subscription-results"
          {{- if .Values.sentry.subscriptionConsumerEvents.autoOffsetReset }}
          - "--auto-offset-reset"
          - "{{ .Values.sentry.subscriptionConsumerEvents.autoOffsetReset }}"
          {{- end }}
          {{- if .Values.sentry.subscriptionConsumerEvents.noStrictOffsetReset }}
          - "--no-strict-offset-reset"
          {{- end }}
          - "--consumer-group"
          - "query-subscription-consumer"
          - "--healthcheck-file-path"
          - "/tmp/health.txt"
        {{- include "sentry.startupProbe.execHealthcheckFile" (dict "startupProbe" .Values.sentry.subscriptionConsumerEvents.startupProbe "healthcheckFile" "/tmp/health.txt") | nindent 8 }}
        {{- include "sentry.livenessProbe.execHealthcheckFile" (dict "livenessProbe" .Values.sentry.subscriptionConsumerEvents.livenessProbe "healthcheckFile" "/tmp/health.txt") | nindent 8 }}
        env:
{{ include "sentry.env" . | indent 8 }}
{{ include "sentry.env.nodestore-s3" . | indent 8 }}
{{- if .Values.sentry.subscriptionConsumerEvents.env }}
{{ toYaml .Values.sentry.subscriptionConsumerEvents.env | indent 8 }}
{{- end }}
        volumeMounts:
        - mountPath: /etc/sentry
          name: config
          readOnly: true
        - mountPath: {{ .Values.filestore.filesystem.path }}
          name: sentry-data
{{ include "sentry.volumeMount.replay-filesystem" . | indent 8 }}
{{ include "sentry.volumeMount.nodestore-s3" . | indent 8 }}
        {{- if include "sentry.gcs.secretName" . }}
        - name: sentry-google-cloud-key
          mountPath: /var/run/secrets/google
        {{ end }}
{{- if .Values.sentry.subscriptionConsumerEvents.volumeMounts }}
{{ toYaml .Values.sentry.subscriptionConsumerEvents.volumeMounts | indent 8 }}
{{- end }}
{{- if .Values.global.volumeMounts }}
{{ toYaml .Values.global.volumeMounts | indent 8 }}
{{- end }}
        resources:
{{ toYaml .Values.sentry.subscriptionConsumerEvents.resources | indent 12 }}
{{- if .Values.sentry.subscriptionConsumerEvents.containerSecurityContext }}
        securityContext:
{{ toYaml .Values.sentry.subscriptionConsumerEvents.containerSecurityContext | indent 12 }}
{{- end }}
{{- if .Values.sentry.subscriptionConsumerEvents.sidecars }}
{{ toYaml .Values.sentry.subscriptionConsumerEvents.sidecars | indent 6 }}
{{- end }}
{{- if .Values.global.sidecars }}
{{ toYaml .Values.global.sidecars | indent 6 }}
{{- end }}
      {{- if .Values.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.serviceAccount.name }}-subscription-consumer-events
      {{- end }}
      volumes:
      - name: config
        configMap:
          name: {{ template "sentry.fullname" . }}-sentry
{{ include "sentry.volume.nodestore-s3" . | indent 6 }}
      - name: sentry-data
      {{- if and (eq .Values.filestore.backend "filesystem") .Values.filestore.filesystem.persistence.enabled (.Values.filestore.filesystem.persistence.persistentWorkers) }}
      {{- if .Values.filestore.filesystem.persistence.existingClaim }}
        persistentVolumeClaim:
          claimName: {{ .Values.filestore.filesystem.persistence.existingClaim }}
      {{- else }}
        persistentVolumeClaim:
          claimName: {{ template "sentry.fullname" . }}-data
      {{- end }}
      {{- else }}
        emptyDir: {}
      {{ end }}
{{ include "sentry.volume.replay-filesystem" . | indent 6 }}
      {{- if include "sentry.gcs.secretName" . }}
      - name: sentry-google-cloud-key
        secret:
          secretName: {{ include "sentry.gcs.secretName" . }}
      {{ end }}
{{- if .Values.sentry.subscriptionConsumerEvents.volumes }}
{{ toYaml .Values.sentry.subscriptionConsumerEvents.volumes | indent 6 }}
{{- end }}
{{- if .Values.global.volumes }}
{{ toYaml .Values.global.volumes | indent 6 }}
{{- end }}
      {{- if .Values.sentry.subscriptionConsumerEvents.priorityClassName }}
      priorityClassName: "{{ .Values.sentry.subscriptionConsumerEvents.priorityClassName }}"
      {{- end }}
{{- end }}
