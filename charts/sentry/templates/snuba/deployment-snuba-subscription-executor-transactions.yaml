{{- if has "feature-complete" .Values.profiles }}
{{- if .Values.snuba.subscriptionExecutorTransactions.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "sentry.fullname" . }}-snuba-subscription-executor-transactions
  labels:
    app: {{ template "sentry.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    app.kubernetes.io/managed-by: "Helm"
  {{- if .Values.asHook }}
  {{- /* Add the Helm annotations so that deployment after asHook from true to false works */}}
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    "helm.sh/hook": "post-install,post-upgrade"
    "helm.sh/hook-weight": "18"
  {{- end }}
spec:
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
        app: {{ template "sentry.fullname" . }}
        release: "{{ .Release.Name }}"
        role: snuba-subscription-consumer-transactions
  replicas: {{ .Values.snuba.subscriptionExecutorTransactions.replicas }}
  template:
    metadata:
      annotations:
        checksum/snubaSettingsPy: {{ .Values.config.snubaSettingsPy | sha256sum }}
        checksum/config.yaml: {{ include "sentry.snuba.config" . | sha256sum }}
        {{- if .Values.snuba.subscriptionExecutorTransactions.annotations }}
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.annotations | indent 8 }}
        {{- end }}
      labels:
        app: {{ template "sentry.fullname" . }}
        release: "{{ .Release.Name }}"
        role: snuba-subscription-consumer-transactions
        {{- if .Values.snuba.subscriptionExecutorTransactions.podLabels }}
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.podLabels | indent 8 }}
        {{- end }}
    spec:
      affinity:
      {{- if .Values.snuba.subscriptionExecutorTransactions.affinity }}
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.affinity | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorTransactions.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.nodeSelector | indent 8 }}
      {{- else if .Values.global.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.global.nodeSelector | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorTransactions.tolerations }}
      tolerations:
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.tolerations | indent 8 }}
      {{- else if .Values.global.tolerations }}
      tolerations:
{{ toYaml .Values.global.tolerations | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorTransactions.topologySpreadConstraints }}
      topologySpreadConstraints:
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.topologySpreadConstraints | indent 8 }}
      {{- end }}
      {{- if .Values.images.snuba.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.images.snuba.imagePullSecrets | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorTransactions.securityContext }}
      securityContext:
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.securityContext | indent 8 }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}-snuba
        image: "{{ template "snuba.image" . }}"
        imagePullPolicy: {{ default "IfNotPresent" .Values.images.snuba.pullPolicy }}
        command:
          - "snuba"
          - "subscriptions-executor"
          {{- if .Values.snuba.subscriptionExecutorTransactions.autoOffsetReset }}
          - "--auto-offset-reset"
          - "{{ .Values.snuba.subscriptionExecutorTransactions.autoOffsetReset }}"
          {{- end }}
          - "--dataset=transactions"
          - "--entity=transactions"
          {{- if .Values.snuba.subscriptionExecutorTransactions.noStrictOffsetReset }}
          - "--no-strict-offset-reset"
          {{- end }}
          - "--consumer-group=snuba-transactions-subscription-executor"
        {{- if .Values.snuba.subscriptionExecutorTransactions.livenessProbe.enabled }}
        livenessProbe:
          exec:
            command:
              - rm
              - /tmp/health.txt
          initialDelaySeconds: {{ .Values.snuba.subscriptionExecutorTransactions.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.snuba.subscriptionExecutorTransactions.livenessProbe.periodSeconds }}
        {{- end }}
        ports:
        - containerPort: {{ template "snuba.port" }}
        env:
{{ include "sentry.snuba.env" . | indent 8 }}
{{- if .Values.snuba.subscriptionExecutorTransactions.env }}
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.env | indent 8 }}
{{- end }}
        envFrom:
        - secretRef:
            name: {{ template "sentry.fullname" . }}-snuba-env
        volumeMounts:
        - mountPath: /etc/snuba
          name: config
          readOnly: true
{{- if .Values.snuba.subscriptionExecutorTransactions.volumeMounts }}
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.volumeMounts | indent 8 }}
{{- end }}
        resources:
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.resources | indent 12 }}
{{- if .Values.snuba.subscriptionExecutorTransactions.containerSecurityContext }}
        securityContext:
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.containerSecurityContext | indent 12 }}
{{- end }}
{{- if .Values.snuba.subscriptionExecutorTransactions.sidecars }}
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.sidecars | indent 6 }}
{{- end }}
{{- if .Values.global.sidecars }}
{{ toYaml .Values.global.sidecars | indent 6 }}
{{- end }}
      {{- if .Values.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.serviceAccount.name }}-snuba
      {{- end }}
      volumes:
      - name: config
        configMap:
          name: {{ template "sentry.fullname" . }}-snuba
{{- if .Values.snuba.subscriptionExecutorTransactions.volumes }}
{{ toYaml .Values.snuba.subscriptionExecutorTransactions.volumes | indent 6 }}
{{- end }}
{{- if .Values.global.volumes }}
{{ toYaml .Values.global.volumes | indent 6 }}
{{- end }}
{{- end }}
{{- end }}
