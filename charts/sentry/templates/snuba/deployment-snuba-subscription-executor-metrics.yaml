{{- if has "feature-complete" .Values.profiles }}
{{- if .Values.snuba.subscriptionExecutorMetrics.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "sentry.fullname" . }}-snuba-subscription-executor-metrics
  labels:
    app: {{ template "sentry.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    app.kubernetes.io/managed-by: "Helm"
  {{- if .Values.asHook }}
  {{- /* Add the Helm annotations so that deployment after asHook from true to false works */}}
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    "helm.sh/hook": "post-install,post-upgrade"
    "helm.sh/hook-weight": "18"
  {{- end }}
spec:
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
        app: {{ template "sentry.fullname" . }}
        release: "{{ .Release.Name }}"
        role: snuba-subscription-executor-metrics
  replicas: {{ .Values.snuba.subscriptionExecutorMetrics.replicas }}
  template:
    metadata:
      annotations:
        checksum/snubaSettingsPy: {{ .Values.config.snubaSettingsPy | sha256sum }}
        checksum/config.yaml: {{ include "sentry.snuba.config" . | sha256sum }}
        {{- if .Values.snuba.subscriptionExecutorMetrics.annotations }}
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.annotations | indent 8 }}
        {{- end }}
      labels:
        app: {{ template "sentry.fullname" . }}
        release: "{{ .Release.Name }}"
        role: snuba-subscription-executor-metrics
        {{- if .Values.snuba.subscriptionExecutorMetrics.podLabels }}
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.podLabels | indent 8 }}
        {{- end }}
    spec:
      affinity:
      {{- if .Values.snuba.subscriptionExecutorMetrics.affinity }}
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.affinity | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorMetrics.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.nodeSelector | indent 8 }}
      {{- else if .Values.global.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.global.nodeSelector | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorMetrics.tolerations }}
      tolerations:
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.tolerations | indent 8 }}
      {{- else if .Values.global.tolerations }}
      tolerations:
{{ toYaml .Values.global.tolerations | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorMetrics.topologySpreadConstraints }}
      topologySpreadConstraints:
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.topologySpreadConstraints | indent 8 }}
      {{- end }}
      {{- if .Values.images.snuba.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.images.snuba.imagePullSecrets | indent 8 }}
      {{- end }}
      {{- if .Values.snuba.subscriptionExecutorMetrics.securityContext }}
      securityContext:
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.securityContext | indent 8 }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}-snuba
        image: "{{ template "snuba.image" . }}"
        imagePullPolicy: {{ default "IfNotPresent" .Values.images.snuba.pullPolicy }}
        command:
          - "snuba"
          - "subscriptions-executor"
          {{- if .Values.snuba.subscriptionExecutorMetrics.autoOffsetReset }}
          - "--auto-offset-reset"
          - "{{ .Values.snuba.subscriptionExecutorMetrics.autoOffsetReset }}"
          {{- end }}
          - "--dataset=metrics"
          - "--entity=metrics_sets"
          - "--entity=metrics_counters"
          {{- if .Values.snuba.subscriptionExecutorMetrics.noStrictOffsetReset }}
          - "--no-strict-offset-reset"
          {{- end }}
          - "--consumer-group=snuba-metrics-subscription-executor"
        {{- if .Values.snuba.subscriptionExecutorMetrics.livenessProbe.enabled }}
        livenessProbe:
          exec:
            command:
              - rm
              - /tmp/health.txt
          initialDelaySeconds: {{ .Values.snuba.subscriptionExecutorMetrics.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.snuba.subscriptionExecutorMetrics.livenessProbe.periodSeconds }}
        {{- end }}
        ports:
        - containerPort: {{ template "snuba.port" }}
        env:
{{ include "sentry.snuba.env" . | indent 8 }}
{{- if .Values.snuba.subscriptionExecutorMetrics.env }}
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.env | indent 8 }}
{{- end }}
        envFrom:
        - secretRef:
            name: {{ template "sentry.fullname" . }}-snuba-env
        volumeMounts:
        - mountPath: /etc/snuba
          name: config
          readOnly: true
{{- if .Values.snuba.subscriptionExecutorMetrics.volumeMounts }}
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.volumeMounts | indent 8 }}
{{- end }}
        resources:
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.resources | indent 12 }}
{{- if .Values.snuba.subscriptionExecutorMetrics.containerSecurityContext }}
        securityContext:
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.containerSecurityContext | indent 12 }}
{{- end }}
{{- if .Values.snuba.subscriptionExecutorMetrics.sidecars }}
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.sidecars | indent 6 }}
{{- end }}
{{- if .Values.global.sidecars }}
{{ toYaml .Values.global.sidecars | indent 6 }}
{{- end }}
      {{- if .Values.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.serviceAccount.name }}-snuba
      {{- end }}
      volumes:
      - name: config
        configMap:
          name: {{ template "sentry.fullname" . }}-snuba
{{- if .Values.snuba.subscriptionExecutorMetrics.volumes }}
{{ toYaml .Values.snuba.subscriptionExecutorMetrics.volumes | indent 6 }}
{{- end }}
{{- if .Values.global.volumes }}
{{ toYaml .Values.global.volumes | indent 6 }}
{{- end }}
{{- end }}
{{- end }}
